import csv

#clase terapeutas
class terapeuta_disponible:
    def __init__(self, terapeuta, dia, horario, sede, especialidad):
        self.terapeuta = terapeuta
        self.dia = dia
        self.horario = horario
        self.sede = sede
        self.especialidad = especialidad

#clase pacientes
class pacientes:
    def _init_(self, nombre, apellidos, cedula, telefono, correo, direccion, terapeuta, razon, notas):
        self.nombre
        self.nombre
        self.apellidos
        self.cedula
        self.telefono
        self.correo
        self.direccion
        self.terapeuta
        self.razon
        self.notas

# clase clinica
class Clinica:
    def __init__(self):
        self.terapeutas_disponibles = {}
        self.pacientes = {}

    def guardar_terapeutas(self):
        with open('informacion.csv', mode='a', newline='') as file:
            writer = csv.writer(file)
            writer.writerow(['Terapeuta', 'Día', 'Horario', 'Sede', 'Especialidad'])
            for terapeuta in self.terapeutas_disponibles.values():
                writer.writerow([terapeuta.terapeuta, terapeuta.dia, terapeuta.horario, terapeuta.sede, terapeuta.especialidad])

    def guardar_pacientes(self):
        Nombre = input("Ingrese el nombre del paciente: ")
        Apellidos = input("Ingrese los apellidos del paciente: ")
        Cedula = input("Ingrese la cédula del paciente: ")
        Telefono = input("Ingrese el teléfono del paciente: ")
        Correo = input("Ingrese el correo del paciente: ")
        Direccion = input("Ingrese la dirección del paciente: ")
        Terapeuta = input("Ingrese el terapeuta del paciente: ")
        Consulta = input("Ingrese el tipo de consulta del paciente: ")
        Notas = input("Ingrese las notas del paciente: ")

        with open('pacientes.csv', mode='a', newline='') as file:
            writer = csv.writer(file)
            writer.writerow([Nombre, Apellidos, Cedula, Telefono, Correo, Direccion, Terapeuta, Consulta, Notas])

        print("Datos ingresados correctamente.")


    def ver_terapeutas(self):
        with open('informacion.csv', mode='r') as file:
            reader = csv.reader(file)
            for row in reader:
                print(row)

    def ver_pacientes(self):
        with open('pacientes.csv', mode='r', newline='') as file:
            reader = csv.reader(file)
            next(reader)
            for row in reader:
                print(row)

clinica = Clinica() 
    
while True:
    print("Escoja el tipo de acceso mediante la selección del número correcto")
    print("1 - Acceso de Servicio al Cliente")
    print("2 - Acceso de Terapeuta")
    acceso = int(input("Seleccione el tipo de acceso: "))

    if acceso == 1:
        print("Que desea hacer?")
        print("1- Ver la disponibilidad")
        print("2 - Agregar disponibilidad")
        opcion = int(input("Seleccione la opción que describe cómo desea proceder: "))
        if opcion == 1:
            clinica.ver_terapeutas()
        elif opcion == 2:
            clinica.guardar_terapeutas()

    elif acceso == 2:
        print("Que desea hacer?")
        print("1 - Ver paciente")
        print("2 - Agregar información de paciente")
        opcion2 = int(input("Seleccione la opción que describe cómo desea proseguir: "))
        if opcion2 == 1:
            clinica.ver_pacientes()
        elif opcion2 == 2:
            clinica.guardar_pacientes()
            break
        else:
            print("La aplicación se ha cerrado")

